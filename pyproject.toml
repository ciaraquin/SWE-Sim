[project]
name = "SWE-Sim"
version = "0.1.0"
description = "Simulation of software engineering projects"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "httpx>=0.28.1",
    "pymongo[srv]>=4.18.1",
]
